{
  "name": "real-life-quest-generator-root",
  "version": "1.0.0",
  "description": "Root package manager for RealLife Quest Generator",
  "private": true,
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend --legacy-peer-deps",
    "install:all": "npm run install:backend && npm run install:frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "ai": "cd ai_service && python app.py"
  },
  "dependencies": {
    "mongoose": "^9.7.0"
  }
}
